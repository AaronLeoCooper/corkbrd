// Corkbrd isolated main script
(function(window){

  var defaults = {

  };

  // Init
  this.Corkbrd = function(opts) {

  };

  Corkbrd.prototype.set = function() {

  };

  function extend(base, extend) {
    for (var k in extend) {
      base[k] = extend[k];
    }
    return base;
  }

  function genHTML() {

  }

})(window);

window.onload = function() {

  var opts = {

  };

  var cb = new Corkbrd(opts);

};
